<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mol.server.mapper.SysUserRoleMapper">

    <resultMap id="BaseResultMap" type="com.mol.server.entity.SysUserRole">
        <result column="user_id" property="userId" />
        <result column="role_id" property="roleId" />
    </resultMap>

    <select id="selectRoleKeysByUserId" resultType="java.lang.String">
        SELECT
            r.role_key
        FROM
            sys_role r
                LEFT JOIN
            sys_user_role ur ON ur.role_id = r.id
        WHERE
            ur.user_id = #{userId}
          AND r.status = '0'      AND r.del_flag = '0'    </select>

</mapper>